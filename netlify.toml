[build]
  command = "npm install && npm run build"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# Note: Redirects are handled in _redirects file
# Static assets (dist/, styles/) are excluded from redirects

